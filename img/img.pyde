def rgb(pixel):
    return (red(pixel), green(pixel), blue(pixel))

def convert(img):
    final = []
    if pixels: print("_")
    for i in range(img.height):
        for j in range(img.width):
            
    
size(600, 600)
img = loadImage("ranger_spritesheet.png")
img.resize(width, height)
background(img)
loadPixels()
